## Requirements

You should install:

* docker for Mac
* localstack

## Start localstack on the Background

```
cd (this directory)
docker-compose up -d
```

## Shutdown localstack

```
cd (this directory)
docker-compose down
```

## Check Docker Container's Status

```docker ps```

